apiVersion: v1
kind: Service
metadata:
  name: {{ include "gke-use-firestore-shared-vpc.fullname" . }}
  labels:
    {{- include "gke-use-firestore-shared-vpc.labels" . | nindent 4 }}
  annotations:
    cloud.google.com/backend-config: '{{ .Values.service.backendConfig | toJson }}'
    cloud.google.com/neg: '{"ingress":true}'
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "gke-use-firestore-shared-vpc.selectorLabels" . | nindent 4 }}

