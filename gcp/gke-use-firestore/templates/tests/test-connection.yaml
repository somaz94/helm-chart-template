apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gke-use-firestore.fullname" . }}-test-connection"
  labels:
    {{- include "gke-use-firestore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gke-use-firestore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
