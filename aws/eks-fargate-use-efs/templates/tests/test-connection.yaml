apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eks-fargate-use-efs.fullname" . }}-test-connection"
  labels:
    {{- include "eks-fargate-use-efs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eks-fargate-use-efs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
