apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eks-fargate-use-ebs.fullname" . }}-test-connection"
  labels:
    {{- include "eks-fargate-use-ebs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eks-fargate-use-ebs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
