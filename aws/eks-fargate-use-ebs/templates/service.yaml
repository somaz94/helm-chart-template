apiVersion: v1
kind: Service
metadata:
  name: {{ include "eks-fargate-use-ebs.fullname" . }}
  labels:
    {{- include "eks-fargate-use-ebs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-fargate-use-ebs.selectorLabels" . | nindent 4 }}
