repositories:
  - name: gitlab
    url: https://charts.gitlab.io

releases:
  - name: build-image
    namespace: luckyday
    chart: gitlab/gitlab-runner
    version: 0.80.0
    values:
      - values/build.yaml